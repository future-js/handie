import { getAnimationDetailView } from '@_/modules/animation/views';

import { createView } from '@/shared/utils';

import context from '../../context';

export default createView(context, getAnimationDetailView());
