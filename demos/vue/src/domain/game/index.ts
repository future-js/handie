import { MODULE_NAME } from '@_/modules/game/helper';

import { ModuleDescriptor } from '@/types';

import * as views from './views';

export default { name: MODULE_NAME, views } as ModuleDescriptor;
