import { ViewDescriptor } from '@/types';

import SpreadsheetForm from './SpreadsheetForm.vue';

export default {
  name: 'SpreadsheetFormView',
  category: 'object',
  widget: SpreadsheetForm,
  fields: [],
} as ViewDescriptor;
